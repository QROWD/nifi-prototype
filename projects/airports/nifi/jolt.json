[{
    "operation": "shift",
    "spec": {
      "@": "info[]"
    }
  },
  {
    "operation": "default",
    "spec": {
      "calibration": "${task.calibration}",
      "priority_0": "${task.priority_0}",
      "n_answers": "${task.n_answers}",
      "quorum": "${task.quorum}",
      "project_id": "${task.project_id}"
    }
  }
]
